# Codex 通用笔记插图提示词

你需要为整个学习笔记项目中的 Markdown 文件补充“教学型插图”。

这是一套**通用规则**，适用于所有章节、所有目录和后续新增笔记。  
不要根据某几个已存在章节建立固定模板，也不要假设后续内容一定属于供应链、PMC、BOM、MRP 等主题。

你的核心任务是：

> **先理解当前笔记内容，再决定是否需要图片、应该画什么、用什么形式表达。**

图片是辅助理解的视觉解释，不是装饰。

---

# 一、处理范围

从当前笔记项目根目录开始，递归检查需要处理的 Markdown 文件。

对每个 Markdown 文件：

1. 先完整阅读当前文件；
2. 理解该文件的主题、章节结构和知识重点；
3. 找出真正值得视觉化的内容；
4. 为每个知识点选择最适合的图片形式；
5. 使用 `image_gen` 生成图片；
6. 将图片保存到**当前 Markdown 文件所在目录的 `.assets/`**；
7. 将 Markdown 图片引用插入当前笔记最合适的位置；
8. 检查图片与正文是否一致。

不要一次性根据整个课程目录预先决定所有图片。

不同章节应根据各自内容独立判断。

---

# 二、最重要的原则

不要按照标题机械配图。

不要因为看到 `##` 或 `###` 就自动生成图片。

只有当图片能够做到以下至少一点时才考虑生成：

- 比文字更直观地解释一个概念；
- 明显降低理解复杂流程的难度；
- 帮助理解多个对象之间的关系；
- 帮助区分容易混淆的概念；
- 帮助理解时间顺序、因果关系或决策逻辑；
- 帮助理解层级、结构、系统或数据流；
- 帮助快速记忆一个经典业务问题；
- 能让读者“一眼看懂”正文中较难理解的部分。

如果文字已经足够清楚，则不要强行生成图片。

核心原则：

> **少而精，而不是多而全。**

---

# 三、禁止根据前面章节限制后续章节

前面章节出现过的图片类型，例如：

- 流程图；
- BOM 结构图；
- Push / Pull 对比图；
- MTS / MTO 对比图；
- PMC 判断流程；

都只能作为示例。

它们不是固定模板，也不是后续章节必须沿用的内容。

处理任何新章节时，都必须重新根据当前笔记内容判断：

```text
当前知识点是什么？
↓
它最难理解的地方是什么？
↓
视觉化能够解决什么问题？
↓
哪种图最适合？
↓
是否真的值得生成？
```

不得因为之前使用过某种图，就强行套用到新内容。

---

# 四、先识别“知识关系”，再选择图片类型

不要先想“我要画流程图”。

先判断当前内容属于哪一种知识关系。

## 1. 概念或主题理解

适合：

- 一个新的大主题；
- 一个抽象概念；
- 一个章节开头需要建立整体认知。

可选择：

- 概念示意图；
- 场景型封面图；
- 简化业务全景图；
- 核心对象关系图。

这类图只表达“这一节在讲什么”，不要塞入全部细节。

---

## 2. 顺序关系

当知识点强调：

- 先做什么；
- 后做什么；
- 一个活动完成后进入下一个活动；

可使用：

- 横向流程图；
- 纵向流程图；
- 阶段流程图；
- 生命周期图。

优先保持单一阅读方向。

---

## 3. 决策关系

当内容强调：

- 如果 A，则执行 B；
- 如果不满足，则进入 C；
- 判断条件不同会产生不同处理路径；

可使用：

- 决策树；
- 判断流程图；
- 少量 Yes / No 分支。

不要制造过多分支。

---

## 4. 对比关系

当内容强调两个或多个概念的区别，例如：

- A 与 B；
- 不同策略；
- 不同模式；
- 不同角色；
- 不同系统职责；

可使用：

- 左右对比图；
- 两栏信息图；
- 三栏对比图；
- 对照关系图。

重点展示“区别”，不要把正文整段搬进图片。

---

## 5. 层级或组成关系

当内容强调：

- 一个对象由哪些部分组成；
- 父子关系；
- 上下层结构；
- 模块组成；
- 分类关系；

可使用：

- 树形结构图；
- 层级图；
- 爆炸结构图；
- 模块结构图。

---

## 6. 时间关系

当内容强调：

- 日期；
- 时间窗口；
- 提前期；
- 阶段推进；
- 滚动周期；
- 版本切换；
- 过去 / 当前 / 未来；

可使用：

- 横向时间轴；
- 日期倒排图；
- 周期图；
- 阶段演进图。

---

## 7. 输入 → 计算 → 输出关系

当内容强调：

- 多个数据经过规则计算；
- 输入经过处理得到结果；
- 计划结果由多个参数共同决定；

可使用：

- 计算链路图；
- 输入 / 处理 / 输出图；
- 数据转换图；
- 简化公式关系图。

不要让 `image_gen` 生成大量精确公式。

---

## 8. 数据流或系统关系

当内容强调：

- 系统之间的数据传递；
- 部门之间的信息流；
- 上下游系统；
- 数据从哪里产生、流向哪里；

可使用：

- 系统架构图；
- 数据流图；
- Swimlane 简化图；
- 系统交互图。

重点是对象与箭头关系。

---

## 9. 多角色协同关系

当内容强调：

- 多部门；
- 多岗位；
- 多系统；
- 多责任主体共同完成一个任务；

可使用：

- 协同关系图；
- 简化泳道图；
- 角色职责图；
- 中心辐射图。

---

## 10. 因果关系

当内容强调：

- 一个问题为什么发生；
- 某个错误会导致什么；
- 一个异常向后传递造成哪些影响；

可使用：

- 因果链；
- 风险传导图；
- 原因 → 结果图；
- 简化鱼骨逻辑图。

---

## 11. 经典问题或业务场景

当正文包含一个典型案例或经典问题时，可以把它转化为：

- 业务场景示意图；
- 问题链路图；
- 冲突关系图；
- 风险场景图。

不要只是把案例原文复制到图片中。

应该提炼关键对象、数量、矛盾和结果。

---

## 12. 循环与闭环

当内容强调：

- 持续循环；
- 反馈；
- PDCA；
- 滚动计划；
- 计划 → 执行 → 反馈 → 调整；

可使用：

- 环形流程图；
- 闭环图；
- 反馈循环图。

---

## 13. 多维关系

当内容需要展示多个因素之间的关系，但不是简单流程时，可使用：

- 矩阵图；
- 四象限图；
- 二维关系图；
- 因素关系图。

只有正文确实存在这些维度时才能使用。

不要为了视觉效果自行创造坐标轴。

---

# 五、后续章节出现新知识时如何处理

如果后续章节出现当前提示词没有列出的知识形式：

不要强行套用已有图片类型。

你可以自行判断更合理的视觉表达形式，只要满足：

1. 忠于正文；
2. 能降低理解成本；
3. 不引入正文中不存在的新结论；
4. 简洁；
5. 适合打印；
6. 适合 Markdown 笔记阅读与打印。

也就是说：

> **图片类型是开放集合，不是固定列表。**

---

# 六、二级标题封面图规则

二级标题下面可以出现概念封面图，但不要默认生成。

只在以下情况考虑：

- 这是一个新的大模块；
- 单靠标题难以形成直观认知；
- 一个简洁场景图能帮助读者建立上下文；
- 本节没有更高价值的流程图、结构图或关系图。

如果该章节已经有：

- 流程图；
- 对比图；
- 数据流图；
- 时间轴；
- 结构图；
- 决策图；

通常不必再额外生成封面图。

---

# 七、图片内容必须来自当前笔记

生成图片时，只能使用当前笔记明确支持的信息。

允许：

- 精简；
- 摘要；
- 将长句转成关键词；
- 将正文结构转成视觉结构。

不允许：

- 添加笔记中不存在的新业务规则；
- 添加新的结论；
- 补充未经正文支持的数据；
- 擅自修改概念定义；
- 为了画面完整而虚构步骤；
- 把模型自己的常识混入图片。

如果当前笔记没有足够信息支撑某张图，则不要生成。

---

# 八、不要让示例污染后续章节

任何 Prompt 示例中的：

- 企业名称；
- 产品名称；
- 流程名称；
- 数字；
- 系统名称；
- 专业术语；

都只属于示例。

调用 `image_gen` 前，必须从**当前 Markdown 文件的当前知识点**重新提取真实内容。

不得把之前章节中的对象带入当前章节。

例如：

如果当前章节在讲库存分析，就不要因为之前章节出现过 BOM，而继续画 BOM。

如果当前章节在讲系统架构，就不要因为之前画过生产流程，而把系统关系错误画成生产流程。

---

# 九、图中文字规则

图片内尽量使用：

- 短词；
- 短句；
- 关键数字；
- 关键节点名称。

避免：

- 大段正文；
- 长段解释；
- 复杂表格；
- 密集小字。

原则：

> 正文解释细节，图片表达结构。

如果一张图片需要塞入很多文字才能讲清楚，说明这个知识点可能不适合使用 `image_gen`。

---

# 十、精确数据和复杂图表的特殊规则

`image_gen` 不适合生成要求高度精确的：

- 大型数据表；
- Excel 风格表格；
- 精确坐标图；
- 精确 KPI 数值图；
- 大量数学公式；
- 复杂甘特图；
- 需要逐字符准确的数据看板。

遇到这种内容时：

优先判断是否可以改成“概念型示意图”。

例如：

```text
Forecast
→ Demand Plan
→ MPS
→ MRP
```

可以使用流程示意图。

但如果正文需要展示：

```text
第1周 83.4%
第2周 91.2%
第3周 87.6%
```

则不要让 `image_gen` 虚构一个精确数据图。

如果无法通过示意图准确表达，则宁可不生成图片。

---

# 十一、统一视觉语言

整个笔记项目的图片视觉风格保持一致，但内容和布局可以变化。

统一风格：

- 现代教材；
- 专业培训资料；
- 极简信息图；
- 白色或极浅背景；
- 深灰文字；
- 深灰 / 黑色线条；
- 1～2 种低饱和强调色；
- 扁平化；
- 少量线性图标；
- 高可读性；
- 高对比度；
- 适合打印。

避免：

- 卡通；
- 二次元；
- 赛博朋克；
- 游戏 UI；
- 复杂 3D；
- 大面积渐变；
- 大面积深色背景；
- 炫光；
- 商业广告海报；
- 过多装饰；
- 水印感元素。

即使黑白打印，也应该能够通过：

- 位置；
- 线条；
- 边框；
- 箭头；
- 形状；
- 标签；

理解关系。

不要只靠颜色区分信息。

---

# 十二、图片比例与构图

图片比例必须由当前知识关系和节点数量决定，**不固定使用 16:9**。

可根据内容选择：

- 少量对象、中心辐射或紧凑对比：接近 4:3、3:2 或正方形；
- 一般流程、两栏对比或时间轴：接近 16:9；
- 节点较多的单向流程、系统链路或长时间轴：可使用 2:1、2.5:1 或更宽的横向画布；
- 层级较深的树形结构、纵向决策关系：可使用纵向或更高的画布；
- 内容较少时不要为了填满宽画布而拉大间距、增加装饰或加入无关元素。

比例选择的目标是让图片嵌入 Markdown 正文后无需过度缩放，文字仍能按照正常阅读大小清晰显示，并适合 A4 打印或导出 PDF。

构图要求：

- 优先选择与知识关系匹配的阅读方向；
- 主要内容位于中心区域；
- 保留四周安全边距；
- 不让文字贴边；
- 不让节点过小；
- 宽图或长图必须通过增加有效内容空间提升可读性，不能只增加空白；
- 纵向图片不得长到需要跨越多个页面才能看完整体关系。

## 字号与 Markdown 阅读层级

图片按计划显示宽度嵌入 Markdown 后，图中文字大小应与笔记正文接近。既不能为了容纳内容缩成小字，也不能因为画面元素少就把文字放大成海报标题。

采用以下相对层级：

- 图片通常不重复 Markdown 已有的二级标题；能省略主标题时优先省略；
- 确需主标题时，其视觉大小不超过图内正文的 1.25～1.4 倍，不要达到海报标题大小；
- 模块或节点标题采用正文加粗效果，约为图内正文的 1.0～1.15 倍；
- 节点说明、数量和业务标签与 Markdown 正文保持接近，不要明显更大；
- 辅助注释约为图内正文的 85%～95%，仅用于次要信息；
- 关键数字可以加粗，但除非数字本身是教学重点，否则不要大幅放大。

选择字号前，先确定图片在笔记中的计划显示宽度：

- 信息密集、节点较多的流程或时间轴：通常按正文宽度的 90%～100% 显示；
- 只有 3～5 个对象的关系图：通常按正文宽度的 65%～85% 显示；
- 内容很少的示意图：可以按正文宽度的 50%～70% 显示，不要强制撑满整行；
- 在较小显示宽度下，仍应以 Markdown 正文字号为基准设计图中文字。

如果一张图片必须依靠大量小字才能放下内容，应拆图、缩减文字或改用正文表格，不要降低字号硬塞。

---

# 十三、复杂度限制

一张图片不要表达太多内容。

建议：

- 一级核心对象 ≤ 8 个；
- 一级流程节点 ≤ 8 个；
- 主色 ≤ 3 种；
- 重要关系尽量只有一个主要阅读方向；
- 避免大量交叉箭头。

如果内容复杂：

不要把全部内容塞进一张图。

可以拆成：

```text
整体关系图
+
局部关键流程图
```

但仍要控制总图片数量。

---

# 十四、图片密度

不要机械规定每章必须几张图。

根据知识密度决定。

通常：

- 简单章节：0～1 张；
- 普通知识章节：1～2 张；
- 流程、系统、复杂计划类章节：2～3 张；
- 极复杂章节：必要时更多，但必须每张都有明确价值。

不要为了“覆盖完整”而给每个三级标题都生成图片。

### 案例与练习文件

`02-案例与练习.md` 默认可以不生成配图。案例条件、学习者回答、计算过程和答案解析优先保留为文字、公式或表格，以确保数据准确并便于复盘。

只有同时满足以下条件时，才考虑在案例与练习中增加图片：

- 图片能够显著纠正一个仅靠文字不容易理解的典型误区；
- 图片不重复 `01-核心概念.md` 已有的知识图；
- 图片不依赖大量精确数字、公式或表格；
- 图片放在题目或解析旁确实能降低理解难度，而不是装饰。

---

# 十五、`image_gen` Prompt 生成规则

每一张图片都必须根据当前知识点单独编写 Prompt。

不要使用一个完全相同的 Prompt 批量生成所有图片。

每张 Prompt 至少明确：

1. 当前知识主题；
2. 图片表达目标；
3. 图片类型；
4. 需要出现的核心对象；
5. 对象之间的关系；
6. 阅读方向；
7. 根据内容选择的画布方向与比例；
8. 图片计划显示宽度，以及图中文字相对于 Markdown 正文的字号层级；
9. 中文；
10. 白色背景；
11. 现代教材 / 企业培训信息图风格；
12. 少元素；
13. 适合打印；
14. 不增加正文不存在的信息；
15. 不使用复杂背景；
16. 不使用大段文字或小字号硬塞内容。

通用 Prompt 结构可以参考：

```text
根据当前学习笔记中的【当前知识点】，生成一张中文教学信息图。

这张图的教学目标是：【这张图应该帮助读者理解什么】。

采用【流程图 / 对比图 / 时间轴 / 结构图 / 数据流图 / 决策图 / 场景示意图 / 其他最合适形式】。

需要准确表达以下内容：
- 【对象或节点1】
- 【对象或节点2】
- 【对象或节点3】
- 【核心关系】

布局要求：
【从左到右 / 从上到下 / 中心辐射 / 左右对比 / 时间轴 / 其他】。

画布比例：
根据【节点数量、层级深度和阅读方向】选择【4:3 / 3:2 / 16:9 / 2:1 / 纵向 / 其他比例】，不要为了统一外观固定成16:9。

字号层级：
图片通常不重复Markdown已有标题；模块标题只采用正文加粗效果，不要做成海报大标题；主要标签和说明接近Markdown正文；辅助注释为正文的85%～95%。同时说明图片计划以正文宽度的【50%～100%】显示，并按这个显示宽度检查字号协调性。

视觉风格：
现代教材、专业培训资料、极简信息图、白色背景、深灰线条和文字、1～2种低饱和强调色、少量简单线性图标、高可读性、适合A4打印。

限制：
只使用当前笔记提供的信息，不增加新的业务规则、步骤、数字或结论；文字尽量使用短词和关键词；避免复杂背景、过多装饰、大量小字和交叉箭头；内容过多时拆图，不得通过缩小字号容纳。
```

其中方括号中的内容必须根据当前知识点动态生成。

---

# 十六、图片保存目录

每一张新生成图片都保存到：

```text
当前 Markdown 文件所在目录/.assets/
```

如果 `.assets/` 不存在，则创建。

例如：

```text
notes/
├── 01-xxx.md
├── 02-xxx.md
├── .assets/
│   ├── xxx-flow.png
│   └── xxx-comparison.png
│
├── module-a/
│   ├── chapter.md
│   └── .assets/
│       └── chapter-overview.png
```

注意：

> `.assets/` 是相对于当前 Markdown 文件所在目录，而不是固定项目根目录。

---

# 十七、Markdown 图片路径

需要占满正文宽度的流程图或时间轴，使用：

```markdown
![图片说明](./.assets/filename.png)
```

例如：

```markdown
![需求到计划的数据流](./.assets/demand-to-plan-data-flow.png)
```

内容较少、无需占满正文宽度的图片，可以使用 Typora 支持的 HTML 控制显示宽度并居中：

```html
<div align="center">
  <img src="./.assets/filename.png" alt="图片说明" width="75%">
</div>
```

宽度应根据内容选择，通常为 `50%`～`85%`。不要仅因为图片源文件像素很大就让它撑满正文宽度。

不要使用绝对路径。

不要引用不存在的图片。

---

# 十八、图片文件命名

使用简洁、有语义的英文文件名。

推荐：

```text
topic-overview.png
topic-flow.png
topic-comparison.png
topic-timeline.png
topic-decision-flow.png
topic-data-flow.png
topic-architecture.png
topic-feedback-loop.png
topic-risk-chain.png
```

具体名称根据当前知识点动态生成。

不要使用：

```text
image-20260918140234019.png
```

这种无法理解内容的文件名。

如果多个笔记共用同一 `.assets/` 目录，文件名必须避免冲突。

---

# 十九、插入位置

图片必须插入到它真正解释的内容附近。

常见位置：

### 情况 1：概念总览

```markdown
## 某个主题

![主题概览](./.assets/topic-overview.png)

正文……
```

### 情况 2：流程解释

```markdown
### 某个关键流程

正文先解释背景……

![关键流程](./.assets/topic-flow.png)

继续解释细节……
```

### 情况 3：概念对比

```markdown
### A 与 B 的区别

简短引入……

![A与B对比](./.assets/a-vs-b.png)

详细解释……
```

不要把所有图片统一放到文件开头或结尾。

---

# 二十、已有图片

遇到 Markdown 中已有图片时：

1. 阅读图片前后的正文；
2. 判断该图片所表达的知识点；
3. 如果已有图片已经承担当前教学作用，则不要重复生成；
4. 不删除现有图片；
5. 不无理由修改已有图片路径；
6. 不无理由重命名旧图片；
7. 新生成图片统一放入 `.assets/`。

如果旧图和新图作用高度重复，优先保留旧图。

---

# 二十一、处理整个笔记项目时的执行方式

如果需要处理多个章节或整个目录，不要一次性根据目录名称生成图片。

正确方式：

```text
扫描 Markdown 文件
↓
逐个处理文件
↓
读取当前文件全文
↓
分析当前章节内容
↓
确定高价值视觉点
↓
为每个视觉点选择最合适形式
↓
生成独立 image_gen Prompt
↓
生成图片
↓
保存到当前文件所在目录/.assets/
↓
插入当前 Markdown
↓
检查
↓
处理下一个文件
```

每个文件都重新分析。

后面的文件不受前面文件内容限制。

前面的图片风格可以继承，但知识内容、图形结构和视觉表达方式必须重新判断。

---

# 二十二、不要修改正文

除以下情况外，不修改原 Markdown 正文：

- 插入图片引用；
- 为确保图片位置合理增加必要空行。

禁止：

- 重写原段落；
- 删除原案例；
- 修改公式；
- 改变标题结构；
- 修改知识结论；
- 自行补课。

你的任务是“增加插图”，不是“重写笔记”。

---

# 二十三、最终检查

每处理完一个 Markdown 文件，都检查：

### 内容

- 图片是否来自当前笔记内容？
- 是否引入了正文中不存在的信息？
- 是否出现概念错误？
- 是否与前后正文一致？

### 教学价值

- 图片是否真的降低了理解难度？
- 是否只是装饰？
- 是否存在重复图片？
- 是否有更适合的图形表达方式？

### 视觉

- 图片方向和比例是否与当前知识关系匹配？
- 是否避免为了统一外观机械使用16:9？
- 是否根据内容选择了合适的显示宽度，而不是让所有图片都撑满正文？
- 按计划显示宽度嵌入Markdown后，模块标题、正文标签和注释的字号是否与笔记正文协调？
- 图片是否重复了Markdown已有标题，或把节点标题做成了海报大字？
- 是否存在明显小于Markdown正文、需要放大才能阅读的文字？
- 是否简洁？
- 是否适合打印？
- 黑白打印是否仍可理解？
- 字是否过多？
- 元素是否过多？

### 文件

- 图片是否真实存在？
- 是否保存到当前 Markdown 所在目录的 `.assets/`？
- Markdown 是否使用 `./.assets/xxx.png`？
- 文件名是否有语义？
- 是否发生文件名冲突？

### 修改范围

- 是否只进行了插图所需的最小修改？
- 是否误改正文？

---

# 二十四、最终核心原则

始终遵循：

> **先读内容，再设计图片。**

> **图片形式由知识关系决定，而不是由章节编号决定。**

> **前面章节的图只提供视觉风格参考，不限制后面章节。**

> **流程用流程表达，差异用对比表达，层级用结构表达，时间用时间轴表达，数据传递用数据流表达，决策用决策图表达；如果有更适合的新形式，可以自行采用。**

> **图片负责让知识“一眼看懂”，正文负责解释细节。**

> **如果图片不能提高理解效率，就不要生成。**
